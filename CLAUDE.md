# AdvoAI — Project Context

Canonical copy. Mirrored verbatim into `AdvoAI-frontend/CLAUDE.md` and `AdvoAI-backend/CLAUDE.md` — update here first, then copy both, in the same commit as the code being described.

## Summary

AdvoAI is a conversational legal assistant for Uzbekistan: hybrid parent-child RAG over the legal corpus (`lex.uz`-sourced codes/constitution), Gemini-based generation, contract drafting, document comparison. Two sibling repos:

- **AdvoAI-backend** — FastAPI. Deploys to a Hugging Face Space via a `space` git remote in addition to `origin` (GitHub).
- **AdvoAI-frontend** — Next.js.

The two deployments are cross-origin (frontend's own domain vs. the HF Space), which shapes several decisions below (auth token delivery, CORS, download proxying).

For implementation detail beyond what's here, see the working plan: `/home/doniyor/.claude/plans/i-need-you-to-mellow-whale.md`.

## Done

- **Sliding JWT renewal** (`app/middleware.py`): `get_current_user`/`require_auth`/`require_admin`/`require_root_admin` now take an optional `Response` and reissue the token when less than 24h of its 72h life remains, returned via `X-Renewed-Token` (already exposed cross-origin — `Access-Control-Expose-Headers: *` in `main.py`) and an updated cookie. Frontend `authFetch.ts` picks up that header on every call and persists the new token; `AuthContext.tsx`'s own mirror cookie now derives its `max-age` from the JWT's real `exp` claim instead of a hardcoded 7 days. Root cause of "logged out constantly": a fixed-lifetime token with no renewal path at all, compounded by the frontend's cookie outliving the token it wrapped.
- **Removed the LLM-authored trilingual sources heading** (`prompts.py`, Mode A instruction 5): the model no longer writes its own "Manbalar / Источники / Sources" section — the frontend's `SourcesDropdown`/`WebSourcesDropdown` (already localized, favicon'd) is the only source list now. Inline natural references ("According to Article 5...") are still fine.
- **Sidebar silent-fallback fixes** (`InsightPanel.tsx`): three paths that used to render a bare, unstructured chunk with no explanation now have explicit states — an empty successful parts fetch is treated as an error; a citation with no id resets stale state instead of leaving a previous citation's parts on screen; an invalid/stale `part_id` still shows the document but with a visible "couldn't locate the exact cited section" notice (new locale key `insight.cited_part_missing` in all 3 languages).
- **True-citation highlighting** (Batch 2): `context_markdown` is rebuilt in `chat.py` right before the LLM call with a `[REF n]` tag per retrieved part, numbered in the exact order `_build_citations` iterates `parent_documents` — this gives the model a stable handle to cite without exposing internal ids. A new UNIVERSAL CORE REGULATION 7 in `prompts.py` has the model end its raw output with a hidden `<!--CITATIONS:[{"ref":1,"quote":"..."}]-->` block (never shown to the user) naming which REFs it relied on and a short verbatim excerpt for each. `_extract_citation_quotes` (`chat.py`) strips that block from the visible answer and resolves ref numbers back to `part_id`s; `_build_citations` then drops any retrieved-but-unquoted part instead of showing it as if it were used, and attaches the `quote` to citations that were. A missing/unparseable block falls back to citing only THIS TURN's own fresh matches (`fresh_ids`), never the whole merged memory window (see Batch 3) — a model formatting slip must never surface a part from turns ago that this specific answer never touched. Frontend (`InsightPanel.tsx`) highlights `quote` when present, falling back to whole-part matching against `text` otherwise. Covered by `tests/test_citation_quotes.py`.
- **Session chunk memory** (Batch 3): new `session_chunk_memory` table (schema V7/V8) caches retrieved parts' real content per session — `part_id`, full `parent_documents`-shaped metadata, `text`, and `message_id` (which chat_messages row fetched/re-matched it). `chat_sessions.chunk_memory_turn` is a per-session counter that advances once per fresh-retrieval (non-conversational) turn only — NOT per message, NOT wall-clock. Per turn: bump the counter → prune anything untouched for `CHUNK_MEMORY_WINDOW` (3) turns → load the surviving window → run vector search with `already_cached` (skips the `document_parts` text fetch in `rag_pipeline.retrieve_context` for ids already in memory — the actual "don't re-hit the DB" half of the original ask) → merge memory + this turn's fresh matches (fresh copies win) into `parent_documents` → upsert only the fresh/re-matched ones (memory-only riders don't get their LRU clock touched) → backfill `message_id` once the user's chat_messages row is pre-saved. Conversational turns read the window (`get_session_chunk_memory`) and inject it as plain prose via a new `memory_markdown` param on `client.ask()`/`CONVERSATIONAL_PROMPT_TEMPLATE`, with a "Session Memory Exception" carve-out in `prompts.py` Mode B's law-quoting ban — no bump, no prune, no citation UI. Guests (no session row) keep today's stateless behavior. `top_k` hard-clamped to 5. `delete_document` sweeps orphaned rows via `sweep_orphaned_chunk_memory`. Web/URL results are never cached here (synthetic non-uuid ids, ephemeral content) — see Findings. Fully verified against the live Neon DB (schema apply, insert/prune/LRU-refresh, and a full route-level round trip with the DB pool live), not just mocked tests — see `tests/test_session_chunk_memory.py` and `tests/test_rag.py`.
- **Autocompaction** (Batch 3b): `delete_oldest_message`/`MAX_WINDOW_MESSAGES` retention is gone — every `chat_messages` row is kept forever, both `sessions.py`'s `/{id}/messages` endpoint and `chat.py`'s per-turn fetch now pull everything not yet summarized (`_unsummarized_messages`, bounded only by a generous `MESSAGE_FETCH_CEILING` safety ceiling shared between the two, not a design cap). `chat_sessions.summarized_through_message_id` (schema V9) tracks the newest message already folded into `session_summary`, so a compaction pass only ever folds genuinely new messages. Trigger: `GeminiClient.estimate_tokens` (a real Gemini `count_tokens` call, gated behind a cheap char-length pre-check so short chats never pay for it) measures `session_summary + chunk-memory text + unsummarized history`; over ~500k (`AUTOCOMPACT_TOKEN_THRESHOLD`), `_background_history_maintenance` finds the oldest still-resident chunk-memory row's `message_id` (the anchor), keeps `AUTOCOMPACT_VERBATIM_BUFFER` (5) messages before it plus everything from the anchor onward verbatim, and folds only the prefix before that into the archive summary via the existing (reworded — see Findings) `summarize_archive`. Reusing `LLM_HISTORY_MESSAGES`'s old fixed-12 replay cap for attachment REHYDRATION specifically (now `GeminiClient.ATTACHMENT_REHYDRATION_WINDOW`, applied independently of the now-unbounded text replay) was a fix the advisor caught before ship — see Findings. Verified end-to-end against the live DB: schema apply, and a real `_background_history_maintenance` run against 30 real inserted messages confirming the exact anchor-minus-buffer boundary math. Tests: `tests/test_autocompaction.py`, `tests/test_history_replay_window.py`.

- **Router-decided web search** (Batch 4): `route_query`'s JSON output gets a new `needs_web` boolean (`ROUTER_SYSTEM_PROMPT` extended with concrete positive/negative examples — recent/changing rules and administrative figures → true, settled codes → false), parsed defensively in `_parse_routing` (absent/malformed → `False`, never silently on). `chat.py`'s web-search gate is now `needs_web AND not is_conversational AND user is not None AND user.web_search_enabled` — `request.use_web_search` is kept on the request model for backward compatibility but no longer read. `users.web_search_enabled` (schema V10, default true) is the account-level VETO, read/written via `PATCH /api/auth/me/web-search`; frontend `AuthContext.setWebSearchEnabled` + `useChatManager`'s `useWebSearch` now derive from `user.web_search_enabled` instead of `localStorage`. Exa's `includeDomains` is hard-restricted to both confirmed domain tiers combined (`web_search.py` `PRIMARY_DOMAINS`/`SECONDARY_DOMAINS`) — see Findings for why "restrict only when clearly regulatory" wasn't implemented as a separate distinction. A scoped prompt instruction (only present when web results exist, not a permanent SYSTEM_PROMPT addition) tells the model to prefer the official tier within those results. Verified end-to-end against the live DB (schema apply + real GET/PATCH round trip on `/api/auth/me/web-search`) and with mocked-router tests for the gating matrix (needs_web × account veto × legacy request flag) — `tests/test_web_search_gating.py`, `tests/test_web_search_domain_bias.py`, plus `needs_web` extraction tests in `tests/test_attachment_pipeline.py`.
- **Batch 4 hardening pass** (post-review, same commit): (1) `custom_router_prompt` (admin `override_prompts_enabled`) replaces `ROUTER_SYSTEM_PROMPT` wholesale, which would have silently dropped `needs_web` forever the moment an admin set one — `GeminiClient.__init__` now appends a self-contained `ROUTER_NEEDS_WEB_APPENDIX` (`prompts.py`) to any custom router prompt and logs a warning when it does. Currently moot in production (`override_prompts_enabled` is `false` on the live DB) but was a live landmine. (2) `perform_web_search`'s Exa→Tavily→Serper fallback used to treat "provider returned `[]`" identically to "provider errored" and fall through — meaning the moment the new domain restriction actually filtered Exa down to zero results, it fell through to Tavily, which has **no** domain restriction, silently defeating Batch 4's whole guarantee. Fixed by distinguishing `None` (error/misconfigured → fall through) from `[]` (genuine empty success → accept, stop); `_search_exa` also no longer defaults a malformed/keyless body to `[]` (which used to look like a genuine empty success) — it now returns `None` so a malformed response falls through too. Tests: `test_web_search_gating.py::test_custom_router_prompt_still_requests_needs_web`, `test_web_search_domain_bias.py::test_exa_zero_results_is_not_treated_as_exa_unconfigured` + `test_exa_failure_falls_through_to_tavily`.
- **Router currency-follow-up carve-out** (post-review): user reported a real follow-up ("bu hozirda ham amaldami?" — "is this still in effect?") never triggered web search. Root cause: `chat.py`'s `needs_web` gate (`needs_web AND not is_conversational AND ...`) is only ever evaluated when the router's `intent` is `legal_rag` — but `ROUTER_SYSTEM_PROMPT`'s own conversational rule ("if the query does NOT introduce a net-new legal concept... flag it as conversational") classified a pronoun-only currency question as `conversational` by construction, since it references an already-discussed subject rather than a new one. The two gates (conversational-vs-legal_rag, then needs_web) were sequential, not orthogonal, so a legitimate freshness question got excluded before `needs_web` was ever asked. Fixed with a targeted EXCEPTION in `ROUTER_SYSTEM_PROMPT`'s classification section (`prompts.py`): a follow-up asking whether an already-discussed subject is still valid/current/in force ("hozir ham amaldami", "hali ham shundaymi", "o'zgarganmi", "is this still valid") now routes to `legal_rag` (resolving the subject from history into `search_query`, same as the existing pronoun-resolution rule) instead of `conversational`, so it reaches the `needs_web` check — a new matching example was added to the NEEDS_WEB examples list too. Deliberately scoped as a narrow classification carve-out rather than changing the conversational/legal_rag architecture itself (a confirmed rule) — the `ROUTER_NEEDS_WEB_APPENDIX` (used when an admin's `custom_router_prompt` replaces the system prompt wholesale) was left untouched since it only adds the `needs_web` field to an already-`legal_rag`-classified turn and has no opinion on conversational classification.

- **Message action redesign + Redo/Edit + Report + Read aloud + Share chat + Files panel + PWA install** (Batches A–G, one combined body of work): see the dedicated section below.
- **Prompts extracted to `app/prompts/*.md`**: `app/services/prompts.py` was a dozen multi-hundred-line triple-quoted strings, hard to find/diff a specific prompt in. Each prompt now lives as its own `.md` file under `app/prompts/<category>/` — `assistant/` (main SYSTEM_PROMPT + the three context-injection templates), `router/` (intent router + its needs_web appendix), `documents/` (compare + draft), `summarization/` (archive summarizer), `misc/` (title generation, ingestion metadata extraction) — grouped so related prompts sit next to each other. `prompts.py` is now a thin loader (`_load(relative_path)`) that reads each file into the same module-level constant name `llm_client.py` already imports — zero call-site changes. Loading is eager at import time (not lazy), so a renamed/missing prompt file fails at startup, the same place a typo'd Python constant used to fail, rather than silently on first use mid-request. Verified every loaded constant byte-for-byte identical to its previous Python-string value (including `TITLE_SYSTEM_PROMPT`, the one prompt with no trailing newline) before switching over; full suite (107 tests) unchanged. New prompt files, or prompts for new features, go under the matching category dir (or a new one) — never back into a Python string.

## To do (ship order)

None — Batches 1–5 and the message-actions/share/PWA batch (A–G) are all complete and pushed.

## Batch 5 — status events + word-chunk replay (revised scope)

Genuine token-level streaming was scoped and rejected first — see the reasoning preserved below, since it still applies to `ask()` itself. What shipped instead: the full answer is still generated server-side in one shot (so tool-call detection, the hidden citation-quote block, and `ref_quotes` all work exactly as before Batch 5), then the *response* is streamed to the client two ways:

- **`status` SSE events** ("searching_corpus" / "searching_web" / "drafting") fire before each slow step (`retrieve_context`, `perform_web_search`, `_run_generate_contract_tool`) so the wait is never silent — this is the actual latency-perception fix, and needed no change to `ask()`.
- **`delta` SSE events** replay the finished answer in ~3-word chunks (`_stream_answer_text`, `chat.py`) with a small `asyncio.sleep` between them, purely for the incremental-reveal UX (styled after Claude's own reveal — word-by-word text plus a small rotating indicator that sits below the text while generating, `GeneratingIndicator` in `MessageBubble.tsx`). This is honestly a replay, not true streaming — documented as such, not hidden.
- `citations` and `done` (terminal, carries session_id/model_used/intent/metadata/attachments) fire after the deltas, since both still depend on the complete answer.
- `POST /api/chat/` is now `StreamingResponse` (`text/event-stream`); session/attachment-ownership validation stays in `ask_advoai` (still returns real 403/500 before the stream starts), everything else moved into `_generate_chat_stream`, whose own try/except turns a mid-generation failure into a terminal `error` SSE event instead of an HTTP error status (locked to 200 once headers are sent). `X-Accel-Buffering: no` is set so an nginx-fronted deployment (the HF Space) doesn't buffer the whole response before delivering it.
- Frontend (`useChatManager.ts`): `sendToBackend` now owns the assistant message end-to-end — creates a placeholder (`isStreaming: true`), reads `res.body.getReader()`, and patches the message in place per SSE event; callers (`handleSendMessage`, the pending-question effect) only need the resolved `session_id` for their own bookkeeping. A message mid-stream is excluded from the localStorage persistence effect (would otherwise thrash on every chunk) and stripped of `isStreaming`/`statusLabel` by `serializeMessages` so a reload can never resurrect a stream nothing will finish.
- Verified: full backend suite (93 tests, including two new SSE-contract tests in `test_chat_route.py`), `tsc --noEmit` clean, and a real end-to-end run against the live local backend (`curl -N` and a Node `fetch()` script using the exact same frame-parsing logic as the frontend) confirming the `status → delta × N → citations → done` event sequence over a real network connection with a real Gemini answer. **Not yet visually verified in an actual browser** — the Claude-in-Chrome extension was unavailable in this session, so the fade-in/indicator UI itself (as opposed to the data flowing correctly) has not been eyeballed. Worth a manual pass before calling the UI polish "confirmed," per the verify-before-done rule below.

## Batches A–G — message action redesign, Redo/Edit, Report, Read aloud, Share chat, Files panel, PWA install

Driven by reference screenshots of Gemini's/Claude's/ChatGPT's chat UIs (plan at `/home/doniyor/.claude/plans/i-need-you-to-mellow-whale.md`), shipped as two commits (backend `fec6aef`, frontend `025c89a`):

- **Message variant chains (Redo + Edit)**: `chat_messages.regenerated_from_id` (always points at the chain's ROOT — never a recursive walk) + `is_active` (schema V11). Redo re-answers an assistant reply's preceding question and inserts a new variant, deactivating the old one — never overwrites or deletes it (per the project's nothing-is-ever-deleted rule). Edit is scoped to only the session's single latest user message (an earlier one would mean true branching — out of scope); saving inserts a new user variant and immediately cascades into the same regenerate flow for the assistant turn that followed. Both reuse `chat.py`'s full `_generate_chat_stream` pipeline via new `existing_user_message_id`/`assistant_root_id`/`fixed_chunk_turn` params — same SSE contract as a normal send, so the frontend's stream-consumption code (`consumeChatStream`, extracted out of `sendToBackend`) is shared by all three call sites. New routes: `POST .../regenerate`, `PATCH .../activate`, `POST .../edit`, `GET .../variants` (sessions.py). Frontend: a ◀ 1/2 ▶ switcher renders once a message has more than one variant (lazily fetched via `fetchVariantInfo`, never on a plain message); user messages get a hover (desktop) / tap-on-bubble (mobile, revised — see the post-ship bug-fix pass below) Edit+Copy row (inline textarea, Cancel/Save) distinct from the assistant row's always-visible Redo/Copy/thumbs/3-dot menu.
- **Audit pass this required** (per the project's "audit before reusing" rule): `get_session_messages`/`_unsummarized_messages` now filter `is_active = true`; `remap_chunk_memory_message` keeps an edited turn's chunk-memory anchor pointing at a row that still renders, since the original row it pointed at is now inactive.
- **Report legal issue**: new `message_reports` table (`user_id NOT NULL` — authenticated-only, matching the guest-gating decision below), `POST /api/chat/{message_id}/report` via `require_auth`. No admin viewer built (not asked for) — the data just sits in a normal table.
- **Read aloud**: Web Speech API, frontend-only — reads the *rendered* plain text (a ref on the markdown output) rather than hand-rolling a markdown stripper; `utterance.lang` derives from the active locale.
- **Real "Share chat"**: `chat_sessions.share_token` (a fresh `secrets.token_urlsafe(24)`, deliberately NOT the session's own id) + `shared_through_message_id` (the "up to this point" boundary). Fully public, no login (user-confirmed). `POST /{id}/share {public}` creates/reuses a token and bumps the boundary to the latest message, or clears the token entirely as a real revoke. New fully-open routes in `public.py`: `GET /shared-chat/{token}` and a file proxy scoped to that session's shared range only (`attachment_belongs_to_shared_range` — a token must not become a skeleton key for an arbitrary `s3_key`). **Variant semantics**: the boundary bounds *extent*, not content — a turn inside the shared range always renders whichever variant is currently active, so an edit/redo made after sharing still shows up on the existing link; only turns added after the boundary stay hidden. New standalone `/share/[token]` page on the **frontend's own domain** (not a bare backend URL — no CORS change needed) reusing `MessageBubble` with a new `readOnly` prop that suppresses every action row and the switcher.
- **Files panel**: a new `GeneratedFilesPanel` — deliberately NOT built on `InsightPanel`'s heavier machinery (citation highlighting, drag-resize, doc viewer), since a flat downloadable list needs none of it. Lists every assistant attachment already sitting in client-side `messages` state — no new fetch.
- **PWA install button**: new `useInstallPrompt` hook wrapping `beforeinstallprompt`, a small download-icon button next to the account row in `Sidebar.tsx`, visible only when the browser actually offers a native prompt (nothing built for iOS Safari, which has no such API — expected, not a gap).
- **Guest-gating decision**: guest sessions have no server-side `chat_messages` row to act on (guest chats are localStorage-only), so Redo/Edit/Report/Share/Files are all gated behind `isAuthenticated` in the UI — a guest keeps today's Copy/thumbs-only row. Cheaper than teaching the backend to tolerate a message id that doesn't exist, and consistent with how `useSessions`' mutators already special-case guests.
- Verified: full backend suite (106 tests, live-DB round trips for variant-chain/report/share behavior), `tsc --noEmit` + `next build` + eslint all clean on every touched/new frontend file, and a second live round trip against the local backend with a real minted JWT exercising send → redo → activate → report → edit-cascade → share-create → fetch-with-no-auth-header → revoke → confirm-404, reading the SSE frames byte-for-byte off the wire to confirm they match what `consumeChatStream` expects. **Not yet visually verified in a live browser** — the Claude-in-Chrome extension was unavailable both this session and for Batch 5 — so hover states, the dropdown menu, and modal interactions have been read/typechecked but not eyeballed. Worth a manual pass before calling the UI polish itself "confirmed."

## Batches A–G — post-ship bug-fix pass (mobile screenshots)

Six issues reported from a real phone against the shipped A–G build; all frontend-only except the last:

- **"Thinking" mark stopped after one loop**: `LoadingMark`'s `loop` prop defaulted off for inline indicators (by design — see its doc comment) on the theory that settling once reads as resolved. Wrong for `GeneratingIndicator` (`MessageBubble.tsx`) and the pre-placeholder spinner (`ChatArea.tsx`): generation regularly outlasts the ~4s clip, so settling mid-wait reads as stuck. Both now pass `loop`; the doc comment was reworded to say so explicitly rather than leaving the old blanket "off by default" reasoning as the only guidance.
- **Generated-file card position**: moved from *above* the text bubble (an early layout choice) to *inside* it, after the answer text and `GeneratingIndicator`, before the citations block's `border-t` separator — matching "files always follow the explanation," and dropping the block's own now-redundant `px-6 md:px-8` since the bubble wrapper already supplies it.
- **Stale file card during Redo/Edit**: `regenerateMessage` and `editMessage` (`useChatManager.ts`) reset `text`/`citations` on the placeholder but not `attachments` — so the old file stayed rendered (looking finished) while a fresh answer was still streaming in. Both now also reset `attachments: []`.
- **File icon + label**: the colored-square-with-extension-text placeholder is now a real per-type icon (IconScout, free/commercial-use, `public/icons/files/{docx,pdf,generic}.svg`) and the label is just the extension (`DOCX`/`PDF`/…) instead of "Word document"/"PDF document".
- **Edit/Copy unreachable on mobile**: long-press targeted the action-row wrapper, which is a ~20px strip when hidden (`opacity-0` still occupies layout) — too small to press-and-hold accurately. Replaced with tap-to-reveal on the bubble itself (`onClick` toggles `showUserActions`) plus a `pointerdown` listener on the whole message container that closes the row on any outside tap — covers "tap elsewhere" and "tap another message" for free, no state lifted into `ChatArea`. Desktop hover (`group-hover`) unchanged.
- **Long chat title overlapping the Files/Share buttons**: `truncate` never engages inside a flex chain unless every ancestor flex item explicitly sets `min-w-0` (a flex item's default `min-width: auto` refuses to shrink below its content). The title's own wrapper div and its button were missing it. Added `min-w-0` down the chain (`ChatArea.tsx` header); the icon-button group already had `flex-shrink-0`.
- **Switching an assistant reply's variant showed a stale, mismatched user prompt above it** (the one bug with a real root cause, not just a missed class): `/activate` (`sessions.py`) only ever flipped `is_active` on the ONE chain it was told to — the paired row of the other role was never touched. After an Edit (which creates both a new user variant AND a new assistant variant), switching the assistant back to an older reply left the just-edited user text rendered above it, since nothing repointed the user chain back to the original. Fixed with a new query, `get_paired_message(session_id, created_at, want_role)` — finds the chronologically-adjacent row of the opposite role (nearest user row strictly BEFORE for an assistant target, nearest assistant row strictly AFTER for a user target), searching ALL rows regardless of `is_active` since a variant's true pairing is fixed at the instant it's created (chat.py's `_generate_chat_stream` is always fed exactly one adjacent row, and nothing else can insert into a session mid-request). `/activate` now calls this after its own flip and activates the paired chain too, symmetric in both directions. Needed `datetime.fromisoformat()` on the created_at string before binding — asyncpg infers the bind type from the SQL-text `::timestamptz` cast and then refuses to encode a plain `str` against it, a trap for any future query taking a timestamp param the same way the rest of this file passes timestamps around (as isoformat strings). Verified against the live DB with a real edit→redo-back→redo-forward round trip (not just the mocked route test, which was also extended to assert the cascade call) confirming the exact original/edited question renders next to whichever reply variant is active.
- Verified: full backend suite (107 tests), `tsc --noEmit` + `next build` + eslint clean, and the live-DB pairing round trip above. **Not visually verified in a live browser** — the Claude-in-Chrome extension was unavailable this session too (three sessions running now with this same gap) — so the tap-reveal, icon swap, and file-card reposition are read/typechecked but not eyeballed. Worth a manual mobile pass before calling this closed.

### Why true token-level streaming (of `ask()` itself) was rejected

The plan's original design predates Batches 2–3 and no longer fits what shipped:

- **`ask()` sends `tools=[generate_contract]` on every single call** — whether a response is prose or a function call is unknown until enough of the stream is consumed to tell, so any implementation must buffer first, not stream from byte one.
- **The hidden `<!--CITATIONS:[...]-->` block (Batch 2) would render to the user** if raw deltas were streamed — it's stripped from the *complete* answer today; streaming needs the same tail-suppression, which means buffering the tail at minimum.
- **Citations can no longer be sent "immediately."** The plan assumed citations were available pre-generation; since Batch 2 they depend on `ref_quotes`, parsed out of the finished answer.
- **`_generate_with_retry`'s key-rotation/backoff ladder retries a *completed* call.** It has no way to retry or resume a half-emitted stream.

Honest token-level streaming here would still mean buffering the full response server-side and replaying it — which is exactly what the status+replay design above does deliberately and transparently, instead of pretending otherwise.

## Findings

- R2 (Cloudflare) has no CORS policy by default — hence the `/api/chat/file/{key}/raw` same-origin proxy route for docx preview/download instead of fetching the presigned URL directly from the browser.
- FastAPI matches routes in registration order; a greedy `:path` route registered early can silently swallow requests meant for a more specific route registered later.
- Retrieval is whole-document-part granularity (parts run 20k+ chars) — a citation's `text` today is the entire part, not the sentence actually used, which is why sidebar highlighting lights up the wrong span (fixed by Batch 2's verbatim-quote approach).
- Retrieved chunk text is never persisted into `chat_messages`/conversation history today — only the model's own prose answer is replayed on follow-up turns (`llm_client.py` history replay). `safe_citations_for_storage` deliberately strips `text` before persisting citations. This is why a naive "just filter by citation id already seen" dedup would have been silently unsafe (Batch 3's `session_chunk_memory` fixes this by storing content, not just ids).
- ~~The existing background task (`chat.py`) permanently deletes rows via `delete_oldest_message`...~~ **Resolved in Batch 3b**: `delete_oldest_message` and `MAX_WINDOW_MESSAGES` are removed entirely (the function no longer exists in `queries.py`); every `chat_messages` row is kept forever. `LLM_HISTORY_MESSAGES`'s old fixed-12 replay cap is gone too — replaced by `_unsummarized_messages` (everything not yet folded into `session_summary`) — so "which of the two knobs" is no longer an ambiguity to track.
- Making the LLM text-replay window unbounded (Batch 3b) has a sharp edge: `llm_client.py`'s history replay rehydrates attachments (`build_parts` — an R2 fetch + Gemini file-handle mint, or a full-text inline) for every historical user turn carrying `document_ids`. Left unbounded, a document-heavy session would re-rehydrate every attachment on every turn until compaction fires at ~500k tokens — real latency/cost, and a plausible 413 well before then. Fixed by decoupling: `GeminiClient.ATTACHMENT_REHYDRATION_WINDOW` (12) bounds *rehydration* independently of the now-unbounded *text* replay. Caught by the advisor before ship, not by a test — worth remembering that "make X unbounded" always deserves an explicit check for what X drags along with it.
- DuckDuckGo is non-viable as a free web-search provider (from prior investigation); Brave has dropped its free tier. Current provider order: Exa → Tavily → Serper.
- The `/full/meta` endpoint (`documents.py`) already returns the authoritative full list of a document's `part_id`s, so the frontend can and does detect a stale/invalid cited `part_id` client-side without a separate backend validation call.
- The DB in `.env` is a live Neon instance, not local — `scripts/run_schema.py` was run against it (with explicit user confirmation) to verify Batch 3's schema; it's additive/idempotent (`CREATE TABLE IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS`), matching the existing V1–V8 convention in `schema_unified.sql`. Note `CREATE TABLE IF NOT EXISTS` is a no-op against an already-created table — a column added to that table's definition later (V8's `message_id`) needed its own separate `ALTER TABLE ADD COLUMN IF NOT EXISTS`, not an edit to the original `CREATE TABLE`.
- Web-search and fetched-URL results carry synthetic, non-uuid `id`s (`web-{provider}-{idx}`, `url-{idx}`) — anything touching `session_chunk_memory`'s `part_id::uuid` column must filter these out first (`_is_uuid` + `kind == "corpus"` in `chat.py`), or Postgres throws on the cast and 500s the whole request. Caught by `tests/test_session_chunk_memory.py::test_web_search_results_are_not_upserted_into_chunk_memory`.
- `rag_pipeline.retrieve_context`'s vector search step can never be skipped (it's what tells you which parts are relevant to THIS question), but the subsequent `document_parts` text fetch can be, per-id, via its `already_cached` param — this is the real "don't re-load the same chunk from the DB" mechanism, distinct from (and in addition to) not re-injecting the same text twice into the prompt.
- The plan's "restrict to the primary domain tier only when clearly regulatory, otherwise don't hard-restrict" distinction was simplified: since web search here only ever fires for `legal_rag`-intent turns (conversational/document_request never trigger it), every web-search-triggered query is regulatory by construction, so both confirmed tiers are always passed to Exa's `includeDomains` together. Also: Exa has no "soft preference" domain parameter — `includeDomains` is a hard filter — so the "prefer official over secondary" ranking is expressed only via the prompt instruction, not the API call. Revisit if a future need arises for genuinely unrestricted web search (e.g. a non-legal-domain feature).
- `get_current_user`/other auth dependencies are called directly as plain functions in several `auth.py` routes (not via FastAPI `Depends()`), so `app.dependency_overrides` can't intercept them in tests — a real signed JWT (`create_access_token` + `Authorization: Bearer`) is required to exercise those routes, not a dependency override.
- Minting a real JWT for a live-DB round trip outside pytest needs `await init_pool()` called first — `get_connection()` raises `RuntimeError` otherwise, since the pool is normally initialized by FastAPI's own startup hook, not on import.
- `regenerated_from_id` always pointing at the chain's ROOT (never the immediately-previous variant) is what keeps "list every variant" a single `WHERE id = root OR regenerated_from_id = root` query — pointing it at the previous sibling instead would need a recursive walk to find every variant, for no benefit.

## Rules

- **Audit existing code against intent before reusing it — never inherit as-is.** Read the mechanism, confirm it still matches what's wanted now, and say plainly if it doesn't. This already caught the `delete_oldest_message` conflict above.
- **Nothing is ever deleted.** Every `chat_messages` row is kept forever; "compaction" means summarizing what's sent to the LLM, never truncating or dropping stored messages. Full history always renders in the UI.
- **No hard character/token ceiling on retrieved legal chunks.** They're structured text (moddas/articles) that must never be truncated mid-clause. Size is controlled by window size (5 messages) and autocompaction (~500k tokens), not by a char cap.
- **Confirmed settings:** `top_k` clamped to 5 per turn; session chunk memory window = **3 fresh-retrieval turns** (`CHUNK_MEMORY_WINDOW` in `queries.py`, revised down from an initial 5 after verifying worst-case memory volume — see Findings), LRU eviction; autocompaction trigger ≈ 500k tokens, preserving the memory window + the 5 messages immediately before its anchor, verbatim; web search is router-decided with the user toggle as veto-only, persisted per-account in the DB; Exa domain bias — primary/official tier `lex.uz, gov.uz, prezident.uz, parliament.gov.uz, senat.uz, minjust.uz, soliq.uz, cbu.uz, stat.uz`, secondary/reputable (prefer, not restrict) `norma.uz, kun.uz, gazeta.uz, spot.uz`.
- **Conversational (memory-grounded) turns keep today's UI**: no citation dropdown, no quote mechanism — the model may write an inline "Manbalar" mention only when genuinely load-bearing, and must never cite anything outside the chunks actually injected into that turn's context.
- **Verify before marking done.** Run the relevant tests, exercise the actual UI/endpoint, don't just read the diff and assume it works — especially for streaming, which must be checked against the real deployed HF Space, not only local dev.
- **Update this file at the end of every batch**, then copy it into both `AdvoAI-frontend/CLAUDE.md` and `AdvoAI-backend/CLAUDE.md` in the same commit as the code it describes.
